<Page
    x:Class="Trabajo_DSI.Configuracion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Trabajo_DSI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <Style TargetType="Pivot" x:Key="Menu">
            <Setter Property="Title" Value="CONFIGURACIÓN"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Pivot Style="{StaticResource Menu}" SelectedIndex="2" HorizontalAlignment="Right">
            <PivotItem Header="Controles">
                <TextBlock Text="all emails go here." />
            </PivotItem>
            <PivotItem Header="Audio">
                <TextBlock Text="unread emails go here." />
            </PivotItem>
            <PivotItem Header="Gráficos" Background="Gray">
                <ListView>
                    <Grid Height="900" Width="1470" Background="Transparent">
                        <StackPanel HorizontalAlignment="Center" Width="800">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBox Text="RESOLUCIÓN" Background="Transparent" BorderBrush="Transparent" Margin="0,0,500,0"/>
                                <SplitButton Width="200">
                                    <SplitButton.Flyout>
                                        <Flyout Placement="Bottom">
                                            <TextBox Text="1920 * 1080" />
                                        </Flyout>
                                    </SplitButton.Flyout>
                                </SplitButton>
                            </StackPanel>
                            
                        </StackPanel>
                        
                        
                    </Grid>
                </ListView>
            </PivotItem>
            <PivotItem Header="Juego">
                <TextBlock Text="urgent emails go here." />
            </PivotItem>
            <PivotItem Header="Accesibilidad">
                <TextBlock Text="urgent emails go here." />
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
